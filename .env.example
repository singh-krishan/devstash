# Neon PostgreSQL — use the POOLED connection URL here (for app queries)
DATABASE_URL="postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/devstash?sslmode=require"

# Neon PostgreSQL — use the DIRECT (non-pooled) connection URL here (for migrations)
DIRECT_URL="postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/devstash?sslmode=require"
